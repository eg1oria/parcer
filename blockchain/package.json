{
  "name": "student-grades-blockchain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node --hostname 127.0.0.1",
    "deploy:localhost": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.6",
    "hardhat": "^3.5.0",
    "typescript": "^5.9.3"
  }
}

